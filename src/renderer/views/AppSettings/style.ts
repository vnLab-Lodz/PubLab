import { styled } from '@mui/material';
import ViewContentBase from '../../components/ViewContent/ViewContent';

export const ViewContent = styled(ViewContentBase)(() => ({
  '.view-content > div': {
    marginBottom: '8.9rem',
  },
}));
